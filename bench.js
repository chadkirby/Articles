// Generated by CoffeeScript 1.3.3
(function() {
  var articlize, fs, i, o, word, words, _i, _len;

  articlize = require('./Articles').articlize;

  fs = require('fs');

  words = ("" + (fs.readFileSync('/usr/share/dict/words', 'utf8')) + "\n" + (fs.readFileSync('/usr/share/dict/propernames', 'utf8')) + "\n" + (fs.readFileSync('/usr/share/dict/connectives', 'utf8'))).split('\n');

  console.log("" + words.length + " words loaded");

  console.time('articlize timer');

  for (i = _i = 0, _len = words.length; _i < _len; i = ++_i) {
    word = words[i];
    o = articlize(word);
  }

  console.timeEnd('articlize timer');

  console.log(articlize('unanticipated result', 'unanimous vote', 'honest decision', 'honeysuckle shrub', '0800 number', '∞ of oregano', 'NASA scientist', 'NSA analyst', 'FIAT car', 'FAA policy'));

}).call(this);
